apiVersion: apps/v1
kind: Deployment
metadata:
  name: forecasting
spec:
  replicas: 1
  template:
    spec:
      serviceAccountName: forecasting-staging-ksa
      containers:
        - name: forecasting
          envFrom:
            - configMapRef:
                name: forecasting-config
            - configMapRef:
                name: forecasting-staging-env-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: forecasting-staging-secrets
                  key: DATABASE_URL
            - name: ADMIN_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: forecasting-staging-secrets
                  key: ADMIN_DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: forecasting-staging-secrets
                  key: JWT_SECRET
            - name: ARGON2_SALT
              valueFrom:
                secretKeyRef:
                  name: forecasting-staging-secrets
                  key: ARGON2_SALT
            - name: IDP_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: forecasting-staging-secrets
                  key: IDP_CLIENT_ID
            - name: IDP_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: forecasting-staging-secrets
                  key: IDP_CLIENT_SECRET
            - name: IDP_ADMIN_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: forecasting-staging-secrets
                  key: IDP_ADMIN_CLIENT_ID
            - name: IDP_ADMIN_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: forecasting-staging-secrets
                  key: IDP_ADMIN_CLIENT_SECRET
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: forecasting-staging-secrets
