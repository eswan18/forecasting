apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: haruspex-staging
resources:
  - ../haruspex-base
  - namespace.yaml
  - service-account.yaml
  - configmap-env.yaml
  - secret-provider-class.yaml
  - haruspex-ingress.yaml
patches:
  - path: deployment-patch.yaml
